APP_NAME="Duta Tunggal ERP"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://example.com

LOG_CHANNEL=stack
LOG_LEVEL=error

# Copy the remaining database, cache, queue, mail, and filesystem settings
# from the deployment secret store. Never commit production credentials.
